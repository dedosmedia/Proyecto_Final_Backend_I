server:
  port: 8888

spring:
  zipkin:
    base-url: http://zipkin:9411/
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dedosmedia/config_server
