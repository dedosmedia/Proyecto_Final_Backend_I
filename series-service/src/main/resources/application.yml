server:
  port: 8083

spring:
  application:
    name: serie-service

  config:
    import: optional:configserver:http://localhost:8888

  data:
    mongodb:
      uri: mongodb://localhost:27017/netflix


